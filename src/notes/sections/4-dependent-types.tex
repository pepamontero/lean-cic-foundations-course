\section{Dependent type theory}

What is this? What is going on?

Types depend on terms. What for? How does this change the type judgement rules?

Why is this interesting? -> Because now we can describe propositions (properties over some objects).

Is this still lambda calculus? How so?

What are pi types and e types? (forall and exists) What even is going on with that?

